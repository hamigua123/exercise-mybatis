<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">


<configuration>
    <!-- 属性 -->
    <properties resource="jdbc.properties">
        <property name="jdbc.username" value="root1"/>
        <property name="jdbc.password" value="root"/>
    </properties>
    <!-- 别名  -->
    <typeAliases>
        <!-- 单个别名定义 -->
        <typeAlias type="com.hmg.mybatis.pojo.User" alias="user"/>
        <!-- 别名包扫描器(推荐使用此方式)，整个包下的类都被定义别名，别名为类名，不区分大小写-->
        <package name="com.hmg.mybatis.pojo"/>
    </typeAliases>


    <!-- 与spring整合后environment配置将废除 -->
    <environments default="test">
        <environment id="test">
            <!-- 使用JDBC -->
            <transactionManager type="JDBC" />
            <!-- 数据库连接池 -->
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}" />
                <!-- <property name="url" value="jdbc:mysql://localhost:3306/mybatis?useSSL=false&serverTimezone=UTC"/> -->
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
    

    <mappers>
      
        <!-- 第三种方式，包扫描器要求(如果有多个xml映射文件,推荐使用此方式)：
             1、映射文件与接口同一目录下
             2、映射文件名必需与接口文件名称一致
        -->
        <package name="com.hmg.mybatis.mapper"/>
    </mappers>
</configuration>